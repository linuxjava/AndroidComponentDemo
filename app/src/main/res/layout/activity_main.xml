<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    android:paddingTop="30dp"
    android:paddingBottom="30dp">

    <Button
        android:id="@+id/btn_androidarchitecture"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="AndroidArchitecture"
        android:textAllCaps="false"
        android:onClick="onAndroidArchitecture"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dagger2"
        android:textAllCaps="false"
        android:onClick="onDagger2"
        app:layout_constraintTop_toBottomOf="@+id/btn_androidarchitecture"/>

</android.support.constraint.ConstraintLayout>